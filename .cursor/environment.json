{
  "name": "nimblytica.com static site",
  "install": "python3 --version",
  "terminals": [
    {
      "name": "static-server",
      "command": "python3 -m http.server 8000",
      "description": "Serves the static site at http://localhost:8000. Open index.html or any demo/*.html page here; pages fetch data/*.fake.json over HTTP."
    }
  ],
  "ports": [
    {
      "name": "web server",
      "port": 8000
    }
  ]
}
